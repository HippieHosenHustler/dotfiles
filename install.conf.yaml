- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: 

- create:
    - ~/Projects

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - ./install_homebrew.zsh
